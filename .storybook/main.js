const config = {
  // Required
  framework: {
    name: '@storybook/react-webpack5',
    options: {}
  },
  stories: ['../stories/*.stories.js'],
  // Optional
  addons: [],
};

export default config;
